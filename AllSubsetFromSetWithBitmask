void subsets(int arr[],int n)
{
    int c=pow(2,n);

    for(int i=0;i<c;i++)
    {
        for(int j=0;j<n;j++)
        {
            if((i&(1<<j))>0)
                cout<<arr[j]<<" ";
        }
        cout<<endl;
    }

}
